mod std;

import (
    std.io
)

/**
 * Created by braxtonn on 11/19/2019.
 *
 *
 */
stable class _nil {
    public override def to_string() : string? = new string("nil");
}

nothing := new _nil();
   private set value -> { } // prevent the user from setting a new value
