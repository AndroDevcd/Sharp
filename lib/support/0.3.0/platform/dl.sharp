mod platform;

import (
    platform.kernel
)

def load_library(name: string) {
    vm.load_library(name);
}

def free_library(name: string): var {
    return vm.free_library(name);
}

def link(name: string, library: string): var {
    return vm.link_func(name, library);
}
