mod std.os;

def sysexec(var[] cmd) : var {
    return __os_exec(cmd);
}

def __os_exec(var[] cmd) : var {
    asm(
        "pushl <cmd>"
        "int 0xe8"
        "return_val %cmt"
        "ret"
    );
}