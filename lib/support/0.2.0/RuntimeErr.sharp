mod std;

/**
 * Created by BraxtonN on 5/17/2018.
 *
 8 this class will be used to throw all runtime exceptions
 */
class RuntimeErr base Exception {
      static Exception e = new Exception("l");

      RuntimeErr(var[] message) {
        base->Exception(message);
      }

      RuntimeErr(string message) {
        base->Exception(message);
      }
}
