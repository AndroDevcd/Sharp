mod main;

def main() {
   NUM_FACTS := 100;
   for(i := 0; i < NUM_FACTS; i++) {
      println("$i is ${factorial(i)}");
   }
}

def factorial(n: var) : var {
    result := 1;
    for(i := 2; i <= n; i++)
       result *= i;
    return result;
}