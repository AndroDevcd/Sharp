mod main;

import ( std.export );

class SerializeTest {
    word := "test";
    nm := 10;
    arry := { 1, 2, 3 };
}

def main() {

   for(i := 0; i < 1_000_000; i++) {
        serialize(new SerializeTest());
   }
   println("done");
}

