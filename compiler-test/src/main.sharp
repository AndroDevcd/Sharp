mod main;

thread_local fark := 9;

feep : int = new int(-1);

def main() {
  new ff().foo(0);
  println("hello, world! $feep");
}

obfuscate {
    ff
}

ll: int = new int(9);

import (
    std.io.*,
    platform,
    platform.kernel
)

chan := new channel<int>(UNLIMITED);
const loop_count := 10000;
const slices := 10;
const n := 0;
counter := new atomic<int>(0);

interface a {
    def poop();
}

interface b {
    def poop();
}


class ff : a,b {
    x : var[];
    y : int;

    override def poop() {

    }

    static def flarky()  {

    }

    static def addUpTo(n: _int8) := 9;

    static def flarkyfoo() := 10;

    static def flarkyfoo2(o: object?) : string {
        return "";
    }

    def foo(n: var) {
        thread_local x := 9;
        new list<int>();
    }
}

class x {

    init(s: string) {
        if(true) {
            
        }
        return;
    }
}


component {
    //single { foo(get()) }
    factory { 2 }

}
