mod main;

thread_local fark := 9;

feep : int = new int(-1);

def main() {
  new ff().foo(9);
  println("hello, world! $feep");
}

ll: int = new int(9);

class ff {
    x : string;
    y : _int8;
    def foo(n: _int8) := n += y;
}

class x {

    init(s: string) {
        if(true) {
            
        }
        return;
    }
}


component {
    //single { foo(get()) }
    //factory { 2 }

}
