mod main;

thread_local fark := 9;

feep : int = new int(-1);

def main() {
  new ff().foo(null);
  println("hello, world! $feep");
}

ll: int = new int(9);

class ff {
    x : _int8;
    y : _int8;

    def foo(n: object?) := n == n;
}

class x {

    init(s: string) {
        if(true) {
            
        }
        return;
    }
}


component {
    //single { foo(get()) }
    //factory { 2 }

}
