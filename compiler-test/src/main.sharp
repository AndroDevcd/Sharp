mod main;

import (
    std.*,
    platform
);

th : thread?;
count := 0;
def main() {
  load_library("libSharpDLL");
  if(get_last_error() == 6) {
     println("equals!");
  } else
     println("not equals!");
}

native def get_last_error(): int;
