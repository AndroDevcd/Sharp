mod app;

import std;

def main(string[] args) : var {
    string s = "0";
    for(var i = 0; i < 100; i++) {
        fib(30);
        // there
    }

}

// recursive fibbinocci
def fib(var n) : var {

    if(n < 2)
        return n;
    return fib(n-1)+fib(n-2);
}
