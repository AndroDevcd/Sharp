mod app;

import std;

def main(string[] args) : var {
    ObjList black_list = new ObjList();

    for(var i = 0; i < 1000000; i++) {
        black_list.add(new Foo());
        // there
    }

}

public class Foo {
    var i = 1234;
    var[] msg = "mckdm";
    var z = 0.932;
}

