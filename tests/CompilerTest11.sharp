mod app;

import std;

def foo() {
print("hello");
}


def foo(fn k(var), var g) {
    k(9);
}

def main(string[] args) : var {
    fn proto (var);

thread_local fn proto2 (var);

//    proto2 = def? (var y) {  };





























































































































































































































































































































    foo(proto3 = def? (var y) {  }, 5); // broken

//    fn someComputation := def ?(int num, int x, int y): var {
//          // we are in a completly seperate code space
//          // this code space knows nothing of the x variable
//          // unless explicitly passed in
//
//          print("num = " + num);
//    ;
    //var v = foo;
    return 0;
}

public static thread_local fn proto3 = def? (var y) {  };

