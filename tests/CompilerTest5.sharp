mod application;

//import std;

public class Start {

    public static def main(object[] args) : var {
        List<var> intArry; // TODO: empty modifiers after methods classes etc. before block call
        List<object> objArry;
    }
}

class X<T> {}

class List<T> base X<T> {
    T[] data;

    public List() {
        data = new T[8];
        data[9] = 9;
    }

    public def add(T data) {

    }

    public def delegate::work(T data) {

    }

    public def delegate::work2(T data);
}