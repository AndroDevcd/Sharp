mod application;

import std;

public class Start {

    public static def main(string[] args) : var {
        List<var> intArry = new List<var>(); // TODO: empty modifiers after methods classes etc. before block call
        intArry.add(9);
    }
}

class List<T> : X {
    T[] data;

    public List() {
        data = new T[8];
    }

    public def add(T data) {

    }

    public def delegate::work(T data) {

    }

    public def delegate::work2(T data);
}