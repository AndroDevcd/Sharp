mod app;

import std;


def main(string[] args) {
    println("in main");
    ArrayList<var> black_list = new ArrayList<var>();
    var count = string.parseInt(args[0]);

    println("starting loops");
    var past = getUtcTimeInMills(),now, tmp;
    for(var i = 0; i < count; i++) {
        black_list.add(i);
        // there
    }
    black_list = null;
    now = getUtcTimeInMills();
    println("list.add()(ms) " + (now-past));
    past = getUtcTimeInMills();

    for(var i = 0; i < count; i++) {
    }

    now = getUtcTimeInMills();
    println("basic for loop(ms) " + (now-past));
    past = getUtcTimeInMills();

    for(int i = 0; i < count; i++) {
    }

    now = getUtcTimeInMills();
    println("int for loop(ms) " + (now-past));
}

