mod app;

import std;

public class Application { // start of every program must be named Application

    public def main(string[] args) : var {
        ArrayList<var> black_list = new ArrayList<var>();

        for(var i = 0; i < 100000; i++) {
            black_list.add(i);
            // there
        }

    }

}