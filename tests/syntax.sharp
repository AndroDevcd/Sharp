mod app;

import std.io;
import std.io.task;

/**
*
*/

times := 0;
public static def main(args: string[]) : var {
    jb := task.builder()
        .with_timeout(500)
        .recurring(1, seconds) = { args: object[] ->
            jb := args[0] as job;

            throw new error("something bad happened");
        };

    thread.sleep(5000);
    return 4;
}
