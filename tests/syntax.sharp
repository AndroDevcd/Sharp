mod main;

import std.io;

class K {

    private funPtr : (var)(var);
    private n: var;
    j : K[];

    def work(arr: var) : K;

    def operator+(n: var) : var {

    }

    init {
        self->j = null;

    }

    static def foo() : var {}
    static def foo(num : var) : var {}
}
