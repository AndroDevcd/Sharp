mod app;

import std.io;
import std.io.task;
import platform.kernel;
import platform;

/**
*
*/

thread_local count : var;
public static def main(args: string[]) {
    new a {};
    new b {};
    set_size(new c {});
}

def set_size(_a : a) {
    _a.size = 9;
}

class a {
    size := new int();
}

class b base a {

}

class c base b {
    name : string;
    static next : c = new c();
}
