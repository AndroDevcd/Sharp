mod app;

import std.io;
import std.io.task;
import platform;

chan := new channel<var>(UNLIMITED);

def main(args: string[]) {
    for(i := 0; i < 10_000_000; i++) {}

//    chan.read_next(500, 0);
//
//    chan.close();
//    thread.sleep(1000);
}
