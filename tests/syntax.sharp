mod main;

import std.io;

class K {

    private funPtr : (var)(var);

    def foo() := new _object_();

    x : object;
    y : var;

    def foo(i : int) {


    }

    init {
        i : int = 0;
        j : long = i;
    }

}
