mod main;

import platform.kernel;
import std.io;

def main() {
    i := 0;
    for(; i < 10000000; i++) {
        try {
            throw new error("err");
        } catch(error) {
        }
    }

    platform.print(i);
}
