mod main;

import std.io;

class K {

    private funPtr : ()(var);

    init {

        when(funPtr) {
             foo -> {
                return 1;
            }
            else -> {
                return 1;
            }
        }
    }

    static def foo() : var {}
    static def foo(num : var) : var {}
}
