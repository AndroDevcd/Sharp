mod main;

import std.io;

class K {

    private funPtr : (var)(var);
    private n: var;
    j : K[];

    def work(arr: var) : K;

    init {
        ptr : ()();
        o : object = L<var>.e.b;
        o  = new L<int>();
        (o as K).n = 9;
    }

}

class L<t> {
    enum e {
        a,
        b
    }

}
