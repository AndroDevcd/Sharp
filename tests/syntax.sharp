mod main;

import std.io;

class K {

    private funPtr : ()(var);

    init {
        asm {
            int 0x2fabb
        }
    }

    static def foo() : var {}
    static def foo(num : var) : var {}
}
