mod main;

import std.io;

class K {

    private funPtr : (var)(var);

    def foo() := new _object_();

    static x : int;
    y : var;

    def foo(i : int) {


    }

    obfuscate -keep {
        foo() : _object_
    }

    static init {
        j : long = x;
    }

}
