mod app;

import std.io;
import std.export;
import std.io.task;
import platform;
import platform.kernel;

class a { ptr: 0x100 refid #0
    num : var;
    obj : var[];
    _a : a;
}

def main(args: string[]) {
   n := {1, 2, 3};
   println(n.num);
   println(n.obj);
   println(n._a.num);
   _export(n, "class.txt");
   n = _import_from("class.txt") as a;
   println(n.num);
   println(n.obj);
   println(n._a.num);
}
