mod app;

import std.io;
import std.io.task;
import std.io.fiber;
import platform;


def main(args: string[]) {
    //list_timer();
    for(i := 0; i < 5; i++) {
        fib := slice({ args ->
           for(i := 0; i < 100_000_000; i++)
           {
           }
           println("1");
           return 0;
        });
        fib.bind(thread.current());
    }

    for(i:= 0; i < 100_000_000; i++)
    {
    }
       println("2");
}
