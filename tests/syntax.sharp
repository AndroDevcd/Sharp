mod main;

import std.io;

class K {

    private funPtr : (var)(var);

    def foo() {
        return;
    }

    init {
        i := 10;
        o : object = null;
        //        This was tested and passed


                lock o -> {
               try {
                   li:
                    name:
                   try {
                        j := 0;
                        goto name;
                   } catch(exception) {
                   } finally {
                       st2 := "finally!!";
                   }
                   k := 0;
               } finally {
                   str := "finally!! yet again";
               }
               }

    }

}
