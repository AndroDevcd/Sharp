mod app;

import std.io;
import platform;

/**
*
*/


def high_level_except()
{
    throw new error("test");
}

static def foo(var x) : var {
   y := 10;
   return 0;
}

def main(args: string[]) : var {
    load_library("libuntitled1");
    foo(4);
    return 4;
}
