mod app;

import std.io;
import std.io.task;
import platform.kernel;

/**
*
*/
public static def main(args: string[]) : var {
    message : string = "hi";
    scheduled_job := task.with_args(new object[] { message }) = { args : object[] ->
        println(args[0] as string);
        for(i := 0; i < 1_000_000; i++) {

        }
        println("done!");
    };
    thread.sleep(1000);
    return 4;
}