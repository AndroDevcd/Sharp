mod app;

import std.io;
import std.io.task;
import platform.kernel;
import platform;

/**
*
*/
native def foo(num: var, home: building);

public static def main(args: string[]) : var {
    load_library("fooTestLib");
    foo(105, new building());
    println("");
    return 4;
}

def call_me(num: var) {
    println("num is $num");
}

enum color {
    red , green, blue
}

class building {
    building() {
        style = red;
    }

    spft : var;
    style : color;
}

