mod main;

import std.io;

class K {

    init {
        list : object[];
        iter := 0;

        try {
            o := list[iter++];
        } catch(exception){

            return 1;
        }
         finally {

        }

        if(iter > 0) {
            return 1;
        } else {
            return 2;
        }
    }
}
