mod app;

import math;

def main(args: string[]) {
    varLst := new list<TestClass>();
    past := utc_mills_time(), now;

    for(i := 0; i < 1200000; i++)
    {
        varLst.add(new TestClass());
    }

    now = utc_mills_time();
    println("ArrayList<var> time (" + (now-past) + ") ms");
    varLst.clear();
}

public class TestClass {
    id: var;
    TAG := "TestClass";
}
