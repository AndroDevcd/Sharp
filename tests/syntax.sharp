mod main;

import std.io;

class K {

    private funPtr : ()(var);

    init {
        2 * 4 ** 10  / 1100 + 5747 % 2 + 100;
    }

    static def foo() : var {}
    static def foo(num : var) : var {}
}
