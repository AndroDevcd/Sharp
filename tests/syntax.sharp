mod app;

import std.io;
import std.io.task;
import platform.kernel;

/**
*
*/
count := 0;
future : deferred<var>;
const IN := 0;
public static def main(args: string[]) : var {
    f := new file("tests/tests/file.sharp");
    string.fmt('l');
    //println("name: ${f.mkdir()}");
    dir := 0;
	f.write(dir == IN ? "in" : "out");
    return 4;
}
