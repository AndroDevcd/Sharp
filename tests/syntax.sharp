mod main;

import std.io;

class K {

    private funPtr : ()(var);

    init {

        dict := {  as<int, string> 200 : "hello" };

        vect := { 100, 120, 1003 };
    }

    static def foo() : var {}
    static def foo(num : var) : var {}
}
