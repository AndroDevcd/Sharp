mod application;

import std;

public enum Enums {
        APPLE = 90999,
        CHERRY = 17,
        PEACH
    };

    public static def foo() : var {
        return 22;
    }

    public static def main(string[] args) : var {
        Enums e = new Enums(Enums.APPLE);
        println("" + e.getName() + " = " + e.getValue());
        println("" + Enums.APPLE.getName() + " = " + Enums.APPLE.getValue());
        asm("nop nop nop nop nop");
        println(" apple = " + Enums.APPLE);
        println(Enums.APPLE);
        print(Enums.APPLE);
        print((var)Enums.APPLE);
    }
