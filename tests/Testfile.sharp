mod app.code;

/**
 * Created by bknun on 8/16/2018.
 */
class TestStruct {
    var a, b, c;

    def foo(Type t) {
        empty(foo);
    }

    def empty(fn ptr (Type)) {}


    def operator=(var data) {
    }
}

def main() {
    TestStruct.Test<var, object> ts = new TestStruct();
    ts.a = 2;
    ts.b = ts.a < 7;
    ts.operator=(9);
    ts.c = ts.a / ts.b + 3.141596341593;
}


