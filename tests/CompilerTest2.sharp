mod application;

class k {
    var[] tt;
}

/**
 * Created by BraxtonN on 5/15/2018.
 */
class Start base k {
    var x;
    var[] iarry;

    public Start() {
        iarry = new var[] { 'h', 'i' };
    }

    public static def foo(object o) {

    }

    public static def main(object[] args) : var {

        object o = new Start();
        ((Start)o).x = 9;

        o = ((Start)o).iarry;
        System.print((var[])o);

        o = 9;
        asm("nop");
        System.print((var)o);

        o = new Start();
        Start oref;
        oref = (Start)o;
        System.print(oref.iarry);

        object[] arry = new Start[1];
        arry[0] = new Start();

        if(o == o) {
            System.println("they are the same" + (_int8)9);
        }

        asm("nop");
        asm("nop");
        asm("nop");
        foo(((Start)o).iarry);
        foo(9);
        return 0;
    } // TODO: enforce return statements
}
