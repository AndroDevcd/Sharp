mod application;

import std;

class k {
    var[] tt;
}

/**
 * Created by BraxtonN on 5/15/2018.
 */
public class Application base k {
    var x;
    var[] iarry;

    public Application() {
        iarry = new var[] { 'h', 'i' };
    }

    public static def foo(object o) {

    }

    public static def main(object[] args) : var {

        object o = new Application();
        ((Application)o).x = 9;

        o = ((Application)o).iarry;
        System.print((var[])o);

        o = 9;
        asm("nop");
        System.print((var)o);

        o = new Application();
        Application oref;
        oref = (Application)o;
        System.print(oref.iarry);

        object[] arry = new Application[1];
        arry[0] = new Application();

        if(o == o) {
            System.println("they are the same" + (_int8)9);
        }

        asm("nop");
        asm("nop");
        asm("nop");
        foo(((Application)o).iarry);
        foo(9);
        return 0;
    } // TODO: enforce return statements
}
